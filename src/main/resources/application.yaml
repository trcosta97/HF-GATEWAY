spring:
  cloud:
    gateway:
      routes:
        - id: user-service
          uri: http://localhost:8082
          predicates:
            - Path=/user/**
            - Path=/user
            - Path=/user/{id}
            - Path=/user/{id}/address
            - Path=/user/{id}
            - Path=/user/{id}
          filters:
            - SetPath=/
